# 参与贡献

`main` 是唯一开发主线。不要往 `main` 上直接推。功能开 `feat/*`，修 bug 开 `fix/*`，发版用 annotated tag，且 tag 必须落在 `main` 上的 commit。不要再把 `v2.5` / `v3.0` 当长期开发分支。

最低支持 Go 1.25；CI 同时测 1.25 和 1.26。

1. 新建分支并保持每个提交只解决一个问题。
2. PR 必须写清改了什么、怎么测、已知限制。
3. 新规则必须包含规则编号、风险等级、证据和修复建议。
4. 提交前运行 `gofmt`、`go test ./...`、`go vet ./...`、`npm test`。
5. 不要把生产数据库凭据或模型密钥提交到仓库。
6. Agent 工具必须保持只读，任何会执行 SQL 或修改审批状态的工具都不会被接受。
