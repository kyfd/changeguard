# 参与贡献

ChangeGuard 使用 `main` 作为开发主线，请通过分支和 pull request 提交修改，不要直接推送到 `main`。最低支持 Go 1.25，CI 测试 Go 1.25 和 1.26。

## 开发流程

1. 从最新 `main` 创建范围明确的分支。功能通常使用 `feat/*`，修复使用 `fix/*`；已有仓库维护分支可以沿用其命名。
2. 每个提交只处理一个问题，不混入无关格式化或生成文件。
3. 为行为变化补充测试，尤其是组织隔离、权限、状态机、幂等、持久化恢复和失败关闭路径。
4. 提交前运行与修改相关的检查；通常至少运行：

   ```bash
   gofmt -w <changed-go-files>
   go test ./...
   go vet ./...
   npm test
   ```

5. 在 pull request 中写明修改内容、受影响的治理或安全约束、验证命令、部署影响、兼容性和回滚方式。

## 规则与证据

新增或修改确定性规则时，应提供稳定规则编号、风险级别、是否阻断、命中证据和整改建议。同一制品与规则版本应产生可重复结果。

静态检查不能描述为运行验证。SQL 只有在隔离 PostgreSQL 中实际执行迁移和回滚后才有运行证据；未执行场景使用 `NOT_RUN` 或 `DEMO_ONLY`。不要用随机、固定或模拟的成功数据填补外部证据缺口。

## 安全边界

- 提交人不能批准自己的变更；组织和应用授权必须由服务端执行。
- CI Gate 的任何超时、错误或摘要不一致都应失败关闭。
- 通行证明文只返回一次，日志、测试夹具和示例不得记录真实 Token。
- Agent 和模型工具必须保持只读，不能执行 SQL、修改审批、签发或消费通行证、部署、回滚或应用升级。
- 不要提交 `.env`、生产凭据、模型密钥、运行状态、数据库转储、生产数据或生成的二进制文件。

## 发布

发布 tag 使用 annotated tag，并指向 `main` 上的 commit。不要把版本分支当作长期开发主线。涉及数据格式或数据库结构的修改必须说明向后兼容和恢复路径，并在适用时完成备份恢复与版本往返验证。
