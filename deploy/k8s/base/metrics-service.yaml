apiVersion: v1
kind: Service
metadata:
  name: dbguard-metrics
  namespace: dbguard
  labels:
    app: dbguard
    app.kubernetes.io/component: metrics
spec:
  selector:
    app: dbguard
  ports:
    - name: metrics
      port: 8080
      targetPort: http
